<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="com.timemachine.ex.user.dao.UserDAO">

	<insert id="insertUser" parameterType="map">
	
		INSERT INTO
			`user`
		(
			`name`
			, `email`
			, `loginId`
			, `password`
			, `university`
			, `createdAt`
			, `updatedAt`
		)
		VALUE 
		(
			#{name}
			, #{email}
			, #{loginId}
			, #{password}
			, #{university}
			, now()
			, now()
		)
	
	</insert>
	
	<select id="selectCountByLoginId" parameterType="string" resultType="int">
		SELECT 
			count(1)
		FROM 
			`user`
		WHERE 
			`loginId` = #{loginId}	
		
	
	</select>
	
	<select id="selectUser" parameterType="map"
	resultType="com.timemachine.ex.user.model.User">
		SELECT 
			`id`
			, `name`
			, `email`
			, `loginId`
			, `password`
			, `university`
			, `createdAt`
			, `updatedAt`
		FROM 
			`user`
		WHERE 
			`loginId` = #{loginId}
			AND `password` = #{password}
			
	
	</select>
	
	<select id="selectUserById" parameterType="int"
	resultType="com.timemachine.ex.user.model.User">
		SELECT 
			`id`
			, `name`
			, `email`
			, `loginId`
			, `password`
			, `university`
			, `createdAt`
			, `updatedAt`
		FROM
			`user`
		WHERE
			`id` = #{id}
	
	
	</select>
	
</mapper>